spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace # parameter ? ??

decorator:
  datasource:
    # p6spy ?? ??
    p6spy:
      enable-logging: true
      multiline: true
      logging: slf4j